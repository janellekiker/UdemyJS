/*
const marksMass1 = 78;
const marksHeight1 = 1.69;
const johnsMass1 = 92;
const johnsHeight1 = 1.95;

const marksBMI1 = marksMass1 / (marksHeight1 * marksHeight1);
console.log(marksBMI1);

const johnsBMI1 = johnsMass1 / (johnsHeight1 * johnsHeight1);
console.log(johnsBMI1);

const markHigherBMI = (marksBMI1 > johnsBMI1);
console.log(markHigherBMI)


const marksMass2 = 95;
const marksHeight2 = 1.88;
const johnsMass2 = 85;
const johnsHeight2 = 1.76;

const marksBMI2 = marksMass2 / (marksHeight2 * marksHeight2);
console.log(marksBMI2);

const johnsBMI2 = johnsMass2 / (johnsHeight2 * johnsHeight2);
console.log(johnsBMI2);

const markHigherBMI2 = (marksBMI2 > johnsBMI2);
console.log(markHigherBMI2)

const marksMass1 = 78;
const marksHeight1 = 1.69;
const johnsMass1 = 92;
const johnsHeight1 = 1.95;

const marksBMI1 = marksMass1 / (marksHeight1 * marksHeight1);
const johnsBMI1 = johnsMass1 / (johnsHeight1 * johnsHeight1);

if (marksBMI1 > johnsBMI1) {
    console.log(`Mark's BMI (${marksBMI1}) is higher than John's (${johnsBMI1}) !`);
} else {
    console.log(`John's BMI (${johnsBMI1}) is higher than Mark's (${marksBMI1}) !`);
}
*/
